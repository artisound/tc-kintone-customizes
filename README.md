# kintoneカスタマイズ for TC by kintone CLI

## 初期設定
```
yarn install
```


## カスタマイズ作成
```
kintone-cli create-template
kintone-cli dev --app-name [アプリ名] --watch --localhost
```

## ビルド & デプロイ
```
kintone-cli build --app-name [アプリ名]
kintone-cli deploy --app-name [アプリ名]
```